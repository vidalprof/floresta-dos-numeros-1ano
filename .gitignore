
# Marcos, ago/2026: *"lembre-se que voce criou documentos que estao armazenados
# Nada de documento, nada de atividade, nada de imagem de atividade. Os .md sao
# a MEMORIA do projeto e a trava de seguranca se o chat travar (palavras do
# fixture do motor (gerado pelo _qa/cobaia.py; nao versionar os 2.4MB)
# fotos de trabalho do portao 5c (as APROVADAS ficam em _qa/_fotos_ok/, versionadas)
# fotos do dossiê do Revisor Final (evidência de trabalho, não código)
# lixo de sistema
# no Git, que e pra voce lembrar das coisas, travas de seguranca"*).
# prova da esteira (regenerada por provar_esteira.sh; nunca publicada)
# saida de workflow (refeita por `recuperar.yml` e `godot-web.yml`)
# ⚠️ SO ENTRA AQUI O QUE UM WORKFLOW REFAZ SOZINHO.
**/_colheita*.json
*.pyc
.DS_Store
Thumbs.db
__pycache__/
_cobaia/
_exemplo/
_godot/build/
_prova*/
_prova30/
_provagavetas/
_provalote/
_qa/_dossie/
_qa/_fotos/
_qa/ferramentas/node_modules/
_qa/ferramentas/package-lock.json
_recuperado/fetched/

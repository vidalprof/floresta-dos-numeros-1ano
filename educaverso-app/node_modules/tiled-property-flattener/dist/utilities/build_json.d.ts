/**
 * Builds the JSON object, converting the Map to an object literal,
 * objects to object literals, and converting the Set or EnumValues to an array.
 */
export declare function buildJSON(value: any): any;
